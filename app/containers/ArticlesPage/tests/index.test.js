// import Library from '../index';
// import Table from 'components/Table';

// import expect from 'expect';
// import { mount } from 'enzyme';
// import React from 'react';

describe('<Library />', () => {
  it('should render the loading indicator when its loading', () => {
    console.log('not impelemented!!!'); // eslint-disable-line no-console

    // const renderedComponent = shallow(
    //   <Library loading />
    // );
    // expect(renderedComponent.contains(<List component={LoadingIndicator} />)).toEqual(true);
  });

  it('should render an error if loading failed', () => {
    console.log('not impelemented!!!'); // eslint-disable-line no-console

    // const renderedComponent = mount(
    //   <Library
    //     loading={false}
    //     error={{ message: 'Loading failed!' }}
    //   />
    // );
    // expect(
    //   renderedComponent
    //     .text()
    //     .indexOf('Something went wrong, please try again!')
    //   ).toBeGreaterThan(-1);
  });

  it('should render the books if loading was successful', () => {
    console.log('not impelemented!!!'); // eslint-disable-line no-console

    // const books = [{
    //   title: 'book 1',
    //   link: 'https://amazon.com/book_1',
    //   author: 'Books Author',
    //   year: 1999,
    // }];

    // const renderedComponent = mount(
    //   <Library
    //     books={books}
    //   />
    // );

    // expect(renderedComponent.contains(<Table items={books} />)).toEqual(true);
  });
});
